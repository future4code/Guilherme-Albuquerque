Crie uma função de lambda que receba um texto e retorne um objeto com dois 
parâmetros. O primeiro é um booleano (isEmail) que deve dizer se o texto é um 
email ou não. Já o segundo é uma string (reason) indicando o motivo daquela 
string não ser um email.

exports.handler = async (event) => {
    const response = {
        
        body: 
        if (event.email.includes("@"){
          return ({
            "isEmail": true,
            "reason": "email valid because contains @"
          }
          )
        } else {
          "isEmail": false,
          "reason": "email not valid because not contains @"
        },
    }
    
    return response;
}